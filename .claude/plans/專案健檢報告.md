# zt-event-gateway 未使用／停用檔案盤點報告

## Context
使用者要求對整個 repo 做逐資料夾、逐檔案的靜態盤點，列出**沒有被使用**或**已經停止使用**的檔案。本次範圍限定在 repo root、`bin/`、`src/`、`tests/`、`anser-gateway/`、`Event-Driven/`、`Sagas/`、`docker/`、`scripts/`、`spiffe/`、`docs/`、`packages/` 以及頂層設定檔；依使用者指示**不深入 `Services/` 子應用**（CodeIgniter 4 sub-apps 視為外部 submodule，`.gitignore` 也已忽略）。本階段**只交付清單**，不刪任何檔案。

驗證方式：三個 Explore agent 平行掃描 → 手動交叉驗證 `composer.json`、`docker-compose*.yml`、`.github/workflows/ci.yml`、`Sagas/OrderSaga.php` imports，確認引用圖。分類定義：

- **DEAD** — 全 repo grep 不到引用（只剩自我 autoload），或被新版明顯取代且名單已過時
- **SUSPECT** — 可能還有用，但 CI / composer scripts / docs 都沒觸及；需人眼確認再決定
- `LIVE` 檔案不列入，此報告只列需要被審視的

---

## DEAD（證據明確，可直接刪除）

| # | Path | 證據 |
|---|---|---|
| 1 | `docker-compose.yml.bak` | `.bak` 備份檔，無任何引用 |
| 2 | `composer.phar` (3.3 MB) | 工具二進位不該入 repo，`.gitignore` 遺漏 |
| 3 | `composer.bat` | Windows wrapper，除了自身無引用 |
| 4 | `initialization.php` | `composer.json:69 mq:init` 唯一指向，但其 event queue 硬編碼名單少了 `OrderSagaCompletedEvent`；實際已被 `bin/worker.php:264` 的 `QueueTopology::setupRequestAndEventQueues()` + `HandlerScanner::scanEventTypesFromFile(Sagas/OrderSaga.php)` 動態取代。誤用會產生錯誤拓撲。 |
| 5 | `scripts/run_all_events.bat` | Windows batch，全 repo 零引用 |
| 6 | `Event-Driven/Events/InventoryDeductRequestedEvent.php` | `Sagas/OrderSaga.php` 未 import、未 publish、未 handler |
| 7 | `Event-Driven/Events/PaymentProcessRequestedEvent.php` | 同上，完全孤立 |
| 8 | `docker/envoy/envoy.yaml` | `docker-compose.yml` / `docker-compose.spiffe.yml` 均無掛載 |
| 9 | `docker/envoy/envoy-spiffe.yaml` | 同上 |
| 10 | `docker/php/Dockerfile` | compose 中無 `build: docker/php`；live 的是 `docker/php-openswoole/` 與 `docker/php-spiffe/` |
| 11 | `spiffe/spire-server/server.conf` | compose 掛載的是 `spiffe/spire-server-e2e/server.conf`，此為舊版 shadow |
| 12 | `spiffe/spire-agent/agent.conf` | compose 掛載的是 `spiffe/spire-agent-e2e/agent.conf`，此為舊版 shadow |
| 13 | `spiffe/image.png` (924 KB) | 無任何 md/doc 引用，孤立資產 |
| 14 | `architecture.mmd` (repo root) | README.md、CLAUDE.md、docs/ 皆無引用；推測被 `docs/architecture-flows.md` 取代 |

**附帶修整**（不是檔案，但同一次 cleanup 應處理）：
- `composer.json` 的 `mq:init` script 跟隨 `initialization.php` 一起刪除，或改成呼叫 `QueueTopology`
- `anser-gateway/config/Filters.php` 引用了不存在的 `App\Filters\{TestFilter, TestFilter2, GlobalFilter}`，清掉該段 alias

---

## SUSPECT（需人工再確認才能刪）

### scripts/
| Path | 疑點 |
|---|---|
| `scripts/e2e-full-stack.sh` | 750 行，與 `scripts/e2e-full-architecture.sh` 功能幾乎重疊（同 7 phase 結構），但 CI / composer.json / CLAUDE.md 都只指 architecture 版 |
| `scripts/diagnose-order-saga.sh` | 401 行 debug 工具，無 CI / composer / docs 引用 |
| `scripts/lsvid-experiment.sh` | 121 行實驗腳本，同類功能由 `spiffe/e2e/test-lsvid-chain.sh` (composer `lsvid:e2e`) 取代 |
| `scripts/start-spiffe.sh` | `docker compose --profile zt up` 可取代 |
| `scripts/stop_all.sh` | `docker compose down` 可取代 |
| `scripts/check_status.sh` | compose healthchecks 可取代 |
| `scripts/run_all_events.sh` | 無任何 reference |

### spiffe/
| Path | 疑點 |
|---|---|
| `spiffe/e2e/test-mtls.php` | 非 CI / composer 引用，看起來是手動實驗腳本 |
| `spiffe/e2e/test-mtls-workload.sh` | 同上 |
| `spiffe/scripts/register-workloads.sh` | 不可執行；live 版是 `register-workloads-internal.sh` |
| `spiffe/helper/*.conf` | 目錄整體未被任何 compose 掛載 |
| `spiffe/proto/workloadapi.proto` | 對應生成檔已存在於 `src/Workload/`；repo 內無 protoc 呼叫流程，保留此檔僅供追蹤 schema |

### docs/
| Path | 疑點 |
|---|---|
| `docs/lsvid-experiment.md` | 內文指向 SUSPECT 的 test-mtls 腳本，資訊過期 |
| `docs/Data-flow.mmd` | README / CLAUDE 無連結 |
| `docs/data/` | 子目錄存在但用途不明；`.gitignore` 已忽略 `lsvid-bench-20*.json` / `stress-*.json`，本身為實驗資料落地處，可確認是否還需要 |

### Event-Driven/
| Path | 疑點 |
|---|---|
| `Event-Driven/Events/OrderCompletedEvent.php` | `Sagas/OrderSaga.php:13` import 但從未實例化、從未 handle；實質為 `OrderSagaCompletedEvent` 的空殼別名 |

### src/ & anser-gateway/
| Path | 疑點 |
|---|---|
| `src/HandlerScannerInterface.php` | 介面定義但 `HandlerScanner` 未 implement；無其他實作類 |
| `anser-gateway/config/Filters.php` | 引用不存在的 App\Filters 測試類，為死設定 |
| `public/index.php` | 舊版 CodeIgniter-style front controller，目前 entrypoint 是 OpenSwoole (`bin/gateway.php`)；需確認是否還供 `composer gateway` (`php -S ... -t public`) 腳本使用（目前 composer.json:66 確有該 script） |
| `Logs/` | 只剩 `.gitkeep`，為 runtime 目錄；若 logging 現走 stdout/monolog handler，可評估是否保留 |

---

## 建議後續動作（供使用者參考，本階段不執行）

1. 先處理 14 個 DEAD 項目（風險極低）
2. SUSPECT 區的 scripts/ 部分可一次移到 `scripts/dev-tools/` 或 `.archive/` 保留 git history
3. `initialization.php` 被刪除時同步刪除 `composer.json` 的 `mq:init` script
4. `docker/envoy/`, `spiffe/spire-{server,agent}/`, `spiffe/helper/`, `spiffe/proto/` 這幾個目錄可整個刪除——但 proto/ 若要保留作為 upstream schema 追蹤來源，可加 README 註明
5. SUSPECT 確認過後，更新 `.gitignore` 加入 `/composer.phar` 以免再次誤 commit

## 驗證方式（若日後要做刪除）

- 刪除後跑 `composer test:unit` → 所有 phpunit 應 pass
- 跑 `composer gateway:e2e`（輕量 E2E，不開 SPIRE） → Saga 7 步驟應完整
- 跑 `COMPOSE_PROFILES=zt composer ci:zt` → 完整 SPIRE + LSVID E2E 應 pass
- 檢查 `.github/workflows/ci.yml` 三個 job 仍綠
