apiVersion: v1
kind: Service
metadata:
  name: {{ include "zt-event-gateway.fullname" . }}
  labels:
    {{- include "zt-event-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zt-event-gateway.selectorLabels" . | nindent 4 }}
